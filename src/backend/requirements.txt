# Quantum Hacker Terminal — Python backend dependencies
#
# Install with:  pip install -r requirements.txt
#
# Core framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
websockets>=12.0

# Qiskit ecosystem
# qiskit ≥ 1.0 introduced the unified package; qiskit-aer is the simulator.
qiskit>=1.1.0
qiskit-aer>=0.14.0

# IBM Quantum hardware access (optional — only needed for mode=real)
# Comment this out if you only want the simulator.
qiskit-ibm-runtime>=0.24.0

# Scientific compute (already pulled in by Qiskit, listed explicitly for clarity)
numpy>=1.26.0
